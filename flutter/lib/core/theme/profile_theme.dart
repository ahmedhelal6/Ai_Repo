import 'package:flutter/material.dart';

class AppColors {
  static const background = Color(0xFF070707);
  static const card = Color(0xFF121212);
  static const primary = Color(0xFFB71C1C);
  static const primarySoft = Color(0xFFFF5252);
  static const text = Colors.white;
  static const subText = Colors.white70;
}